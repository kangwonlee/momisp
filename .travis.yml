language : shell

env:
  - PYTHON=3.6

os :
  - linux

cache :
  pip: true

install:
  # pip update
  - pip3 install --upgrade pip
  - pip3 install -r req.$PYTHON.txt

script: cd tests && py.test -n auto
